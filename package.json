{
  "name": "vibe-secure-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Secure vibe coding platform — a BYOK AI web-app builder instrumented for security research",
  "workspaces": [
    "shared",
    "sandbox-runtime",
    "server",
    "web"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -k -n server,web -c blue,magenta \"npm run dev --workspace @vibe/server\" \"npm run dev --workspace @vibe/web\"",
    "build": "npm run build --workspace @vibe/web",
    "start": "npm run start --workspace @vibe/server",
    "test": "vitest run",
    "e2e": "node e2e/smoke.mjs",
    "typecheck": "tsc --noEmit -p shared && tsc --noEmit -p server && tsc --noEmit -p web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "playwright-core": "^1.61.1",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
