{
  "name": "earworm",
  "version": "0.1.0",
  "description": "Hum a melody, get real sheet music. In-browser hum-to-notes transcription: YIN pitch detection, editable score, playback, MIDI export.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "fixtures": "vite-node scripts/make-fixtures.ts",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "vexflow": "^4.2.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^26.1.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
