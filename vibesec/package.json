{
  "name": "vibesec",
  "version": "0.1.0",
  "description": "Multi-agent security audit framework for vibe-coded projects: a deterministic rule engine grounds Claude-powered Auditor/Verifier/Fixer agents.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "vibesec": "dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "audit:fixture": "npm run build && node dist/cli.js audit test/fixtures/vulnerable-app --offline"
  },
  "keywords": [
    "security",
    "vibe-coding",
    "static-analysis",
    "claude",
    "agents",
    "audit"
  ],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.201"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
